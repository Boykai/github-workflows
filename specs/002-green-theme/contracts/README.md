# API Contracts: Green Theme Option

**Feature**: Green Theme Option  
**Branch**: `002-green-theme`  
**Date**: 2026-02-14  
**Phase**: 1 - Design

## Overview

This directory contains the TypeScript interface definitions and type contracts for the green theme feature. These contracts define the data structures used throughout the implementation.

---

## Type Definitions

All TypeScript interfaces and types are defined in `types.ts`.

## Hook Contract

The `useAppTheme` hook contract is defined in `hook-contract.ts`.

## CSS Contract

CSS custom properties and class naming conventions are documented in `css-contract.md`.

---

## Files in this Directory

1. **types.ts** - Core TypeScript type definitions
2. **hook-contract.ts** - React hook interface specification
3. **css-contract.md** - CSS variable and class name conventions
