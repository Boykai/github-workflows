# Implementation Plan: Onboarding Spotlight Tour & Help/FAQ Page

**Branch**: `042-onboarding-help-faq` | **Date**: 2026-03-15 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/042-onboarding-help-faq/spec.md`

## Summary

Add a celestial-themed guided spotlight tour (9 steps) that auto-launches on first login and a `/help` route with FAQ accordion, feature guides, and tour replay. The tour uses CSS mask cutouts to highlight existing layout elements (sidebar, chat, theme toggle) with positioned tooltips. All state is client-side via localStorage. No backend changes. All new components reuse the existing celestial CSS animation system and theme tokens.

## Technical Context

**Language/Version**: TypeScript ~5.9.0, React ^19.2.0  
**Primary Dependencies**: React Router ^7.13.1, Tailwind CSS ^4.2.0, Lucide React ^0.577.0, class-variance-authority ^0.7.1, Radix UI (tooltip ^1.2.8, slot ^1.2.4)  
**Storage**: localStorage (client-side only, no backend persistence)  
**Testing**: Vitest ^4.0.18 (happy-dom), Playwright ^1.58.2, @testing-library/react ^16.3.2, jest-axe ^10.0.0  
**Target Platform**: Modern browsers (Chrome, Firefox, Safari, Edge), responsive 375px–1920px+  
**Project Type**: Web application (frontend-only feature)  
**Performance Goals**: Tour overlay renders in <100ms, Help page loads in <2s, no layout shift during tour transitions  
**Constraints**: No new npm dependencies, all animations via existing CSS keyframes, <10KB gzipped total bundle addition  
**Scale/Scope**: 9 tour steps, 1 new page, 12 FAQ entries, 8 feature guide cards

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Status | Evidence |
|-----------|--------|----------|
| **I. Specification-First** | PASS | Spec complete with 5 prioritized user stories, GWT acceptance scenarios, 18 FRs, 7 SCs |
| **II. Template-Driven** | PASS | Spec follows canonical template; this plan follows plan template |
| **III. Agent-Orchestrated** | PASS | Plan generated by `/speckit.plan` agent; tasks will be generated by `/speckit.tasks` |
| **IV. Test Optionality** | PASS | Tests not mandated in spec; will include unit tests for `useOnboarding` hook and integration test for tour flow as they guard critical localStorage logic |
| **V. Simplicity/DRY** | PASS | No new dependencies; reuses existing CSS classes, theme system, component patterns, and localStorage persistence pattern from `useSidebarState` |

## Project Structure

### Documentation (this feature)

```text
specs/042-onboarding-help-faq/
├── plan.md              # This file
├── research.md          # Phase 0: decisions on spotlight approach, FAQ structure
├── data-model.md        # Phase 1: Tour step schema, FAQ entry schema
├── quickstart.md        # Phase 1: Developer quickstart for adding tour steps
├── contracts/           # Phase 1: Component prop interfaces
└── tasks.md             # Phase 2 output (/speckit.tasks)
```

### Source Code (repository root)

```text
solune/frontend/src/
├── hooks/
│   └── useOnboarding.ts              # NEW — tour state + localStorage persistence
├── components/
│   ├── onboarding/
│   │   ├── SpotlightOverlay.tsx      # NEW — full-viewport mask with cutout
│   │   ├── SpotlightTooltip.tsx      # NEW — positioned tooltip with content
│   │   ├── SpotlightTour.tsx         # NEW — orchestrator (step definitions + rendering)
│   │   └── TourProgress.tsx          # NEW — dot-based step indicator
│   └── help/
│       ├── FaqAccordion.tsx           # NEW — collapsible FAQ grouped by category
│       └── FeatureGuideCard.tsx       # NEW — clickable card linking to feature page
├── pages/
│   └── HelpPage.tsx                   # NEW — /help route page
├── assets/
│   └── onboarding/                    # NEW — 9 SVG line-art icons
│       ├── sun-moon.svg
│       ├── compass-rose.svg
│       ├── star-chart.svg
│       ├── chat-stars.svg
│       ├── constellation-grid.svg
│       ├── orbital-rings.svg
│       ├── celestial-hand.svg
│       ├── sun-moon-toggle.svg
│       └── book-stars.svg
├── constants.ts                       # MODIFY — add Help to NAV_ROUTES
├── App.tsx                            # MODIFY — add /help lazy route
├── layout/
│   ├── AppLayout.tsx                  # MODIFY — mount SpotlightTour, add data-tour-step attrs
│   ├── Sidebar.tsx                    # MODIFY — add data-tour-step attrs to nav/selector/brand
│   └── TopBar.tsx                     # MODIFY — add data-tour-step to theme toggle (unused props removed)
└── test/
    └── (optional test files)
```

**Structure Decision**: Frontend-only feature using existing web application layout. All new files go under `solune/frontend/src/`. New components follow existing directory patterns: `components/onboarding/` for tour, `components/help/` for Help page sub-components, `pages/` for the new route page. SVG icons in `assets/onboarding/` follow the existing `assets/avatars/` pattern.

## Complexity Tracking

> No constitution violations. No complexity justifications needed.
