# Contracts: Fix Playwright Auth Setup, Mutation-Testing CI Noise, and Deprecated Chores Hook Usage

**Status**: N/A

This feature involves no API changes. All backend endpoints (`/chores`, `/chores/paginated`, `/auth/me`) remain unchanged. The frontend hook migration changes only the client-side query pattern — the HTTP contract is identical.

No OpenAPI schema changes, no GraphQL schema changes, no new endpoints.
