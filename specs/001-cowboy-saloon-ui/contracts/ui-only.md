# API Contracts: Cowboy Saloon UI

This feature is strictly a UI/CSS theming effort.

**Zero API modifications are required.**

The application continues to use existing `AvailableAgent` responses from the backend (`GET /workflow/agents`) where the existing `slug` properties are intercepted on the frontend to inject Saloon-themed icons.
