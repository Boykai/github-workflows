# Implementation Plan: Security, Privacy & Vulnerability Audit

**Branch**: `001-security-review` | **Date**: 2026-03-15 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/001-security-review/spec.md`

## Summary

Address 21 OWASP Top 10 security findings (3 Critical, 8 High, 9 Medium, 2 Low) identified in a security, privacy, and vulnerability audit. Research against the live codebase revealed that approximately 19 of 21 findings were already remediated. The remaining gaps (invalid encryption key handling, dev login verification, OAuth scope documentation, explicit `server_tokens off`, rate limit coverage audit) have been closed. All findings are now verified — 20 fully remediated, 1 documented as a known limitation (OAuth `repo` scope required for GitHub Projects V2).

**Approach**: Verification-first — systematically confirmed each existing remediation, then implemented targeted fixes for the remaining gaps.

## Technical Context

**Language/Version**: Python 3.12+ (backend), TypeScript 5.9 (frontend)
**Primary Dependencies**: FastAPI 0.135+, React 19, nginx 1.27-alpine, slowapi, cryptography (Fernet)
**Storage**: SQLite (aiosqlite, WAL mode) at `/var/lib/solune/data/settings.db`
**Testing**: pytest + pytest-asyncio (backend), Vitest + Playwright (frontend)
**Target Platform**: Linux containers (Docker Compose), browser SPA
**Project Type**: Web application (backend + frontend monorepo under `solune/`)
**Performance Goals**: N/A — security hardening, no new performance-sensitive code
**Constraints**: Zero-downtime for already-remediated items; migration path required for encryption enforcement on existing deployments
**Scale/Scope**: 21 security findings across ~30 files; 2–5 require code changes, remainder require verification only

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

### Pre-Research Gate (Phase 0)

| # | Principle | Status | Notes |
|---|-----------|--------|-------|
| I | Specification-First Development | ✅ PASS | `spec.md` complete with 8 user stories, 28 FRs, acceptance scenarios, edge cases |
| II | Template-Driven Workflow | ✅ PASS | All artifacts follow canonical templates from `.specify/templates/` |
| III | Agent-Orchestrated Execution | ✅ PASS | Plan generated by `speckit.plan` agent; handoff to `speckit.tasks` for Phase 2 |
| IV | Test Optionality with Clarity | ✅ PASS | Tests not explicitly mandated in spec; verification is behavior-based (10-item matrix). Tests are optional per constitution — verification matrix serves as acceptance criteria. |
| V | Simplicity and DRY | ✅ PASS | Security hardening modifies existing patterns, no new abstractions introduced. Centralized `verify_project_access` dependency is already DRY. |

**Gate result**: ✅ ALL PASS — proceed to Phase 0.

### Post-Design Gate (Phase 1)

| # | Principle | Status | Notes |
|---|-----------|--------|-------|
| I | Specification-First Development | ✅ PASS | Research confirms spec findings against live code; no spec changes needed |
| II | Template-Driven Workflow | ✅ PASS | `research.md`, `data-model.md`, `contracts/`, `quickstart.md` all generated |
| III | Agent-Orchestrated Execution | ✅ PASS | Plan complete; ready for `speckit.tasks` handoff |
| IV | Test Optionality with Clarity | ✅ PASS | No tests mandated; verification matrix (10 behavior-based checks) serves as acceptance criteria |
| V | Simplicity and DRY | ✅ PASS | Remaining gaps are minimal, surgical fixes — no new abstractions or complexity |

**Gate result**: ✅ ALL PASS — proceed to Phase 2 (task generation).

## Current State Assessment

### Audit Finding Status Matrix

| # | Finding | Severity | OWASP | Status | Action Required |
|---|---------|----------|-------|--------|-----------------|
| 1 | Session token in URL | Critical | A02 | ✅ Remediated | Verified |
| 2 | At-rest encryption not enforced | Critical | A02 | ✅ Remediated | Fixed: invalid key raises ValueError in production |
| 3 | Frontend container runs as root | Critical | A05 | ✅ Remediated | Verified |
| 4 | Project resources not scoped | High | A01 | ✅ Remediated | Verified |
| 5 | Timing attack on Signal webhook | High | A07 | ✅ Remediated | Verified |
| 6 | Missing HTTP security headers | High | A05 | ✅ Remediated | Fixed: explicit `server_tokens off` added |
| 7 | Dev endpoint PAT in URL | High | A02 | ✅ Remediated | Verified: POST body only via DevLoginRequest |
| 8 | OAuth overly broad `repo` scope | High | A01 | ℹ️ Known limitation | Documented: required for GitHub Projects V2 |
| 9 | Session secret no entropy check | High | A07 | ✅ Remediated | Verified |
| 10 | Docker services on all interfaces | High | A05 | ✅ Remediated | Verified |
| 11 | No rate limiting | Medium | A04 | ✅ Remediated | Verified: all endpoints covered |
| 12 | Cookie Secure flag not enforced | Medium | A02 | ✅ Remediated | Verified |
| 13 | Debug bypasses webhook verification | Medium | A05 | ✅ Remediated | Verified |
| 14 | API docs exposed via DEBUG | Medium | A05 | ✅ Remediated | Verified |
| 15 | SQLite dir world-readable | Medium | A02 | ✅ Remediated | Verified |
| 16 | CORS origins not validated | Medium | A05 | ✅ Remediated | Verified |
| 17 | Data volume inside app dir | Medium | A05 | ✅ Remediated | Verified |
| 18 | Chat history in localStorage | Medium | A02 | ✅ Remediated | Verified |
| 19 | GraphQL error messages exposed | Medium | A09 | ✅ Remediated | Verified |
| 20 | GH Actions broad permissions | Low | Supply chain | ✅ Remediated | Verified |
| 21 | Avatar URLs no domain validation | Low | A03 | ✅ Remediated | Verified |

**Summary**: 20 fully remediated and verified, 1 known limitation (OAuth `repo` scope — documented with justification).

## Implementation Phases

### Phase 1 — Critical Fixes (Completed)

#### Task 1.1: Harden Invalid Encryption Key Handling ✅

**Finding**: #2
**File**: `solune/backend/src/services/encryption.py`
**Resolution**: `EncryptionService` now accepts a `debug` parameter. In production mode (`debug=False`), an invalid `ENCRYPTION_KEY` raises `ValueError`. In debug mode, it falls back to plaintext with a warning.
**Note**: Validation occurs at service instantiation time (lazy, on first use via `session_store._get_encryption_service()`), not at application startup. For a true startup gate, validation would need to move to `Settings` initialization. This is documented as a known design trade-off.

#### Task 1.2: Verify Dev Login Endpoint ✅

**Finding**: #7
**File**: `solune/backend/src/api/auth.py`
**Resolution**: Verified — `POST /api/v1/auth/dev-login` accepts `DevLoginRequest` model with `github_token` field in POST body. No URL query parameters accepted. Endpoint returns 404 when `DEBUG=false`.

### Phase 2 — High Priority Hardening (Completed)

#### Task 2.1: Explicit `server_tokens off` in nginx ✅

**Finding**: #6
**File**: `solune/frontend/nginx.conf`
**Resolution**: Explicit `server_tokens off;` directive added to the nginx configuration.

#### Task 2.2: Document OAuth `repo` Scope Limitation ✅

**Finding**: #8 (known limitation)
**File**: `solune/backend/src/services/github_auth.py`
**Resolution**: Comment added explaining `repo` scope is required for GitHub Projects V2 issue write operations. Documented as a known limitation — removing it would break write operations.

#### Task 2.3: Audit Rate Limit Coverage ✅

**Finding**: #11
**Files**: `solune/backend/src/api/chat.py`, `agents.py`, `workflow.py`, `auth.py`
**Resolution**: Verified — rate limits applied to all sensitive endpoints: chat (10/min), agents (5/min), workflow (10/min), OAuth callback (20/min per-IP).

### Phase 3 — Comprehensive Verification Pass

Execute the 10-item verification matrix from the spec to confirm all remediations are effective:

| # | Verification Check | Method |
|---|-------------------|--------|
| 1 | No credentials in browser URL after login | Code review: `auth.py` cookie flow, `useAuth.ts` URL cleanup |
| 2 | Backend refuses to start without `ENCRYPTION_KEY` | Code review: `config.py` production validation |
| 3 | Frontend container non-root | Code review: `frontend/Dockerfile` USER directive |
| 4 | Unauthorized project access returns 403 | Code review: `verify_project_access` dependency on all endpoints |
| 5 | WebSocket rejects unauthorized project | Code review: WebSocket handler project access check |
| 6 | All secret comparisons constant-time | Code review: `webhooks.py`, `signal.py` — `hmac.compare_digest` usage |
| 7 | Security headers present in nginx | Code review: `nginx.conf` header directives |
| 8 | Rate limits return 429 | Code review: `rate_limit.py` + endpoint decorators |
| 9 | No message content in localStorage after logout | Code review: `useChatHistory.ts` in-memory-only design |
| 10 | DB dir 0700, file 0600 | Code review: `database.py` permission setting |

### Phase 4 — Low Priority Items (Verification Only)

- **Finding 20** (GH Actions permissions): Already scoped with `permissions: {}` default + per-job grants. ✅ Verified.
- **Finding 21** (Avatar URL validation): Already validates HTTPS + `avatars.githubusercontent.com`. ✅ Verified.

## Project Structure

### Documentation (this feature)

```text
specs/001-security-review/
├── plan.md                          # This file
├── spec.md                          # Feature specification (8 user stories, 28 FRs)
├── research.md                      # Phase 0: audit reconciliation & current-state analysis
├── data-model.md                    # Security-relevant entity model
├── quickstart.md                    # Verification quickstart guide
├── contracts/
│   └── security-contracts.md        # Security behavior contracts
├── checklists/
│   └── requirements.md              # Spec quality checklist (complete)
└── tasks.md                         # Phase 2 output (NOT created by /speckit.plan)
```

### Source Code (repository root)

```text
solune/
├── backend/
│   ├── Dockerfile                   # Non-root appuser ✅
│   ├── src/
│   │   ├── config.py                # Production validation ✅ (gap: invalid key fallback)
│   │   ├── main.py                  # ENABLE_DOCS toggle ✅
│   │   ├── api/
│   │   │   ├── auth.py              # Cookie-based sessions ✅ (verify: dev login)
│   │   │   ├── tasks.py             # verify_project_access ✅
│   │   │   ├── projects.py          # verify_project_access ✅
│   │   │   ├── settings.py          # verify_project_access ✅
│   │   │   ├── workflow.py          # verify_project_access ✅
│   │   │   ├── webhooks.py          # HMAC verification, no debug bypass ✅
│   │   │   ├── signal.py            # hmac.compare_digest ✅
│   │   │   ├── chat.py              # Rate limited ✅ (audit coverage)
│   │   │   └── agents.py            # Rate limited ✅ (audit coverage)
│   │   ├── middleware/
│   │   │   └── rate_limit.py        # slowapi integration ✅
│   │   └── services/
│   │       ├── encryption.py        # Fernet encryption ⚠️ (gap: invalid key fallback)
│   │       ├── database.py          # 0700/0600 permissions ✅
│   │       ├── github_auth.py       # OAuth scopes ⚠️ (document repo scope)
│   │       └── github_projects/
│   │           └── service.py       # Error sanitization ✅
│   └── tests/
├── frontend/
│   ├── Dockerfile                   # Non-root nginx-app ✅
│   ├── nginx.conf                   # Security headers ✅ (gap: explicit server_tokens off)
│   └── src/
│       ├── hooks/
│       │   ├── useAuth.ts           # URL cleanup, cookie sessions ✅
│       │   └── useChatHistory.ts    # In-memory only, no localStorage ✅
│       └── components/
│           └── board/
│               └── IssueCard.tsx    # Avatar URL validation ✅
├── docker-compose.yml               # 127.0.0.1 bindings, /var/lib/solune/data ✅
```

**Structure Decision**: Existing web application structure (backend + frontend monorepo under `solune/`). No structural changes required — all modifications are within existing files.

## Key Decisions

| Decision | Rationale | Alternatives Rejected |
|----------|-----------|----------------------|
| Verify-first approach | ~19/21 findings already fixed; avoid unnecessary rework | Rewrite-from-scratch (wasteful) |
| Invalid key = startup failure in production | Silent plaintext fallback defeats encryption purpose | Warning-only (current behavior — insufficient) |
| `repo` scope remains as known limitation | GitHub Projects V2 requires it for issue writes | Remove scope (breaks core functionality) |
| Explicit `server_tokens off` | Defense in depth — don't rely on distribution defaults | Trust alpine defaults (fragile) |
| Rate limit coverage audit | Ensures all sensitive endpoints are protected | Trust current coverage (may have gaps) |

## Dependency Order

```
Phase 1 (Critical)
  Task 1.1: Harden encryption key validation ── no deps
  Task 1.2: Verify dev login endpoint ── no deps
  ↓
Phase 2 (High)
  Task 2.1: server_tokens off ── no deps
  Task 2.2: Document OAuth scope ── no deps
  Task 2.3: Audit rate limit coverage ── no deps
  ↓
Phase 3 (Verification)
  All 10 verification checks ── depends on Phase 1 + 2 completion
  ↓
Phase 4 (Low — already verified)
  Findings 20, 21 ── no action required
```

All Phase 1 tasks can run in parallel. All Phase 2 tasks can run in parallel. Phase 3 depends on completion of Phases 1 and 2.

## Risk Assessment

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| Invalid key startup failure breaks existing deployments | Medium | High | Only applies in production mode (`DEBUG=false`); operators already need valid keys per config validation |
| OAuth scope narrowing breaks write operations | High | High | Deferred — documented as known limitation; requires staging validation |
| Rate limit false positives blocking legitimate users | Low | Medium | Per-user limits (not per-IP) avoid shared NAT/VPN penalties |
| `server_tokens off` breaks nginx | Very low | Low | Standard, well-tested directive |

## Complexity Tracking

> No constitution violations — no complexity justifications needed.

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| (none) | — | — |
